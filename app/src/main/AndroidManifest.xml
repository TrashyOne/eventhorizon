<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eventhorizon"
        android:extractNativeLibs="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Eventhorizon">

        <layout
            android:defaultWidth="450dp"
            android:defaultHeight="800dp" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TweaksActivity"
            android:exported="false"
            android:label="AIO Tweaks"
            android:theme="@style/Theme.Eventhorizon" />

        <activity
            android:name=".AppsActivity"
            android:exported="false"
            android:label="Install Apps"
            android:theme="@style/Theme.Eventhorizon" />

        <activity
            android:name=".LedColorActivity"
            android:exported="false"
            android:label="Custom LED Color"
            android:theme="@style/Theme.Eventhorizon" />

        <activity
            android:name=".TerminalActivity"
            android:exported="false"
            android:label="Root Terminal"
            android:theme="@style/Theme.Eventhorizon"
            android:windowSoftInputMode="adjustResize" />

        <service
            android:name=".DnsBlockerService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
                <action android:name="com.veygax.eventhorizon.START_VPN" />
                <action android:name="com.veygax.eventhorizon.STOP_VPN" />
            </intent-filter>
        </service>

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
    </application>

</manifest>